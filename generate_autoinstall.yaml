---
- name: test
  hosts: all
  become: true
  gather_facts: false
  strategy: linear
  tasks:

    - name: Generate autoinstall for cloud-init
      include_role:
        name: autoinstall
        tasks_from: generate_autoinstall.yaml
