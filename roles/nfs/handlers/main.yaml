---
- name: Enable /etc/exports
  ansible.builtin.command:
    cmd: exportfs -ra
  listen: reload exports

- name: Restart NFS server
  ansible.builtin.service:
    name: "{{ nfs_server_service[ansible_facts['os_family']] }}"
    state: restarted
  listen: restart nfs